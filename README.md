# Istanbul Metro Tracker

A web-based map that visualizes the Istanbul rail network based on schedules. 

This project takes static GTFS schedule data from IBB and turns it into a live animation. By calculating the scheduled train times and mapping them to the physical geometry of the tracks, it estimates and displays exactly where the subways and trams are currently located along their routes.
